# Open Emacs client
control + Return
    emacsclient -n -c

# Open web browser
control + shift + Return
    librewolf

# Open terminal emulator
alt + Return
    alacritty

alt + space
    dmenu_run -b -i
